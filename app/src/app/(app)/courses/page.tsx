import type { Metadata } from "next";
export const metadata: Metadata = { title: "Courses | AquaticPro" };
export default function CoursesPage() {
  return <div><h1 className="text-2xl font-bold">Courses (LMS)</h1>{/* TODO Phase 3: port LMS courses component */}</div>;
}
