import type { Metadata } from "next";
export const metadata: Metadata = { title: "Dashboard | AquaticPro" };
export default function DashboardPage() {
  return <div><h1 className="text-2xl font-bold">Dashboard</h1>{/* TODO Phase 0.10: port DashboardPage component from WordPress plugin */}</div>;
}
